# System Overview

This repository contains a Next.js frontend with a chat UI and an MCP tool stub. The purpose of the system is to provide a simple UI for sending messages to an MCP endpoint which will route to your model runtime.

High-level goals:
- Provide an interactive chat on the homepage
- Provide a local MCP tool that accepts messages and returns structured replies
- Store system specifications and derived sprint tasks under `docs/`
