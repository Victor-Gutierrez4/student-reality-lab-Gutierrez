# Sprint: Features Implementation

Tasks (ordered):
1. Implement chat UI component and wire to `/api/chat`.
2. Implement server API to forward to MCP (with timeout/fallback).
3. Build and test MCP stub; ensure JSON contract.
4. Document API contract in `docs/specs` and add examples.
5. Run QA checks: verify messages, error handling, and specs alignment.

Notes: After completing tasks, run through the spec checklist and correct any mismatches.
