# MCP Tool (minimal stub)

This is a minimal Express-based MCP stub that accepts POST /mcp and replies with a canned response. Run via:

```bash
cd mcp-tool
npm install
npm start
```

Replace the internals of `index.js` to integrate with your chosen model runtime.
